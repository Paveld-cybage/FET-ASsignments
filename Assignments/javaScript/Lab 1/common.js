function addNumbers(headParam, bodyParam) {
    const num1 = parseInt(headParam);
    const num2 = parseInt(bodyParam);
    const res = num1 + num2;
    document.write("Addition of " + headParam + " and " + bodyParam + " is: ");
    document.write(res)
}